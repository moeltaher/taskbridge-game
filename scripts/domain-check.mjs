import assert from 'node:assert/strict';
import {scenarios} from '../assets/js/data/scenarios.js';
import {axes,parties} from '../assets/js/data/parties.js';
import {authorityModel,authorityLeaders,significantAuthorities} from '../assets/js/data/authority-model.js';
import {powerTargets} from '../assets/js/data/power-targets.js';
import {questionRef} from '../assets/js/data/question-references.js';
import {leaders,powerAxisCredit} from '../assets/js/core/power-scoring.js';
import {buildPaymentSettlement,operatingCost} from '../assets/js/domain/payment.js';
import {assessAccessDecision} from '../assets/js/domain/access.js';
import {evidenceFor} from '../assets/js/domain/evidence.js';
import {relationshipQuestions,questionsForState,scoredQuestionsForState,acceptedQuestionReferences} from '../assets/js/domain/questions.js';
import {analysisAxes,evidenceDimensions,powerMapComplete,scoreAnalysis} from '../assets/js/domain/analysis.js';
import {intersectionOverUnion,scoreWork,firstTaskOutcome,dataAnswerCredit,semanticDataAnswer,createSampleSequence,samplePoolSize,qualityAfterTask} from '../assets/js/domain/work.js';
import {dataTargetForScene} from '../assets/js/data/data-scenes.js';
import {computeManagedAccess,buildSecondOffer,completeSecondTask,monitorDecision,BREAK_MINUTES,BREAK_STRESS_REDUCTION} from '../assets/js/domain/management.js';
import {reviewAppeal,selectReviewTask,disputeConsequences,applyDisputeOutcome} from '../assets/js/domain/dispute.js';
import {riskTransition} from '../assets/js/domain/risk.js';
const close=(a,b,e=1e-9)=>assert.ok(Math.abs(a-b)<e,`${a} != ${b}`);
assert.deepEqual(parties,['worker','platform','client']);
for(const [type,sc] of Object.entries(scenarios)){assert.ok(sc.accessPolicy.premiumAt>sc.initial.access);const excellentAccess=computeManagedAccess(sc,{completedTasks:[{score:100}],workScore:100,acceptance:100});assert.equal(buildSecondOffer(sc,excellentAccess).premium,true);const seq=createSampleSequence(type,1234);assert.equal(new Set(seq).size,seq.length);assert.equal(seq.length,samplePoolSize(type))}
for(const [type,model] of Object.entries(authorityModel))for(const [axis,config] of Object.entries(model)){assert.equal(Object.values(config.distribution).reduce((a,b)=>a+b,0),100);assert.equal('mediator' in config.distribution,false);assert.deepEqual(powerTargets[type][axis],config.distribution);assert.deepEqual(authorityLeaders(type,axis),leaders(config.distribution))}
assert.deepEqual(significantAuthorities('moderation','price',{within:15}),['platform','client']);assert.deepEqual(questionRef.moderation.price,['سلطة مشتركة بين المنصة والعميل']);assert.deepEqual(questionRef.ai.allocation,['سلطة مشتركة بين المنصة والعميل']);assert.deepEqual(questionRef.translation.allocation,['سلطة مشتركة بين المنصة والعميل']);assert.deepEqual(questionRef.data.price,['المنصة']);
assert.equal(relationshipQuestions.length,7);assert.equal(scoredQuestionsForState({}).length,6);assert.deepEqual(acceptedQuestionReferences('data').parties,['العامل + المنصة + العميل']);assert.deepEqual(analysisAxes({noWorkEnding:true}).map(a=>a.id),['price','allocation','termination']);assert.deepEqual(evidenceDimensions({contractDeclineEnding:true}),['contract','access']);assert.equal(evidenceFor('clientQuality',scenarios.data,{}).dimension,'quality');
const noWorkPayment=buildPaymentSettlement(scenarios.data,{grossWorker:0,clientPaid:0,hold:0});assert.equal(noWorkPayment.mediator,0);assert.equal(noWorkPayment.transfer,0);close(noWorkPayment.net,-operatingCost(scenarios.data));const paid=buildPaymentSettlement(scenarios.data,{clientPaid:5.8,grossWorker:2.1,hold:.21});assert.ok(paid.net<paid.cashPayout);assert.equal(assessAccessDecision(scenarios.data,{noWorkEnding:true,rejections:2}).outcome,'warning');
const target0=dataTargetForScene(0);close(intersectionOverUnion(target0,target0),1);const semanticAnswers=[semanticDataAnswer(0,'leftCenter','standard'),semanticDataAnswer(1,'rightCenter','standard'),semanticDataAnswer(2,'center','standard')];assert.equal(scoreWork(scenarios.data,semanticAnswers,{sampleIndexes:[0,1,2]}),100);assert.equal(qualityAfterTask(90,60),80);
const firstState={sampleSequence:[0,1,2,3,4,5],sampleCursor:0,currentTaskSampleIndexes:[0,1,2],completedTasks:[],workAnswers:semanticAnswers,quality:91,selectedJob:{id:'core',title:'مهمة',duration:12,pay:2.1,clientValue:5.8,stress:10},stress:24,time:0,paidTime:0,grossWorker:0,clientPaid:0};const firstTask=firstTaskOutcome(scenarios.data,firstState);assert.equal(firstTask.score,100);assert.equal('qualityAfterFirstTask' in firstTask.changes,false);
const secondBase={...firstState,...firstTask.changes,secondOffer:{id:'second-premium',title:'دفعة مميزة',pay:3.85,duration:18,clientValue:8,premium:true,sampleCount:3,stress:16},offerDecisionResult:{accepted:true},currentTaskSampleIndexes:[3,4,5],secondTaskAnswers:[semanticDataAnswer(3,'left','short'),semanticDataAnswer(4,'right','short'),semanticDataAnswer(5,'center','wide')]};assert.equal(completeSecondTask(scenarios.data,secondBase).changes.completedTasks.length,2);assert.equal(monitorDecision({stress:42},true).stressAfter,42-BREAK_STRESS_REDUCTION);assert.equal(BREAK_MINUTES,3);
const riskBase={riskSeed:98765,time:12,extraWorkTime:0,stress:30,riskEvent:null,completedTasks:[{score:40}],rejections:0,breakTime:0},riskA=riskTransition(scenarios.data,riskBase),riskB=riskTransition(scenarios.data,{...riskBase,completedTasks:[{score:100}],rejections:8,breakTime:15});assert.equal(riskA.event.roll,riskB.event.roll);
const reviewState={completedTasks:[{id:'task-1',score:92,pay:2,answers:['A'],sampleIndexes:[0]},{id:'task-2',score:55,pay:3.85,answers:['A'],sampleIndexes:[6]}]};assert.equal(selectReviewTask(reviewState).id,'task-2');const target=dataTargetForScene(0),borderline={...target,x:target.x+target.w*.2};assert.ok(dataAnswerCredit(0,borderline)>=.55&&dataAnswerCredit(0,borderline)<.85);const dataAppeal={completedTasks:[{id:'task-1',score:10,pay:2,answers:[borderline],sampleIndexes:[0]}],reviewTaskId:'task-1',initialReviewSeverity:1,appealGround:'guideline'};assert.equal(reviewAppeal(scenarios.data,dataAppeal).accepted,true,'data appeal must follow actual geometry, not task score');assert.equal(reviewAppeal(scenarios.data,{...dataAppeal,appealGround:'technical'}).accepted,false,'technical appeal needs a recorded technical issue');const major=disputeConsequences({...reviewState,reviewTaskId:'task-2'},2);close(major.hold,.693);assert.equal(applyDisputeOutcome({qualityBeforeDispute:80,completedTasks:[{id:'task-1',pay:2}],reviewTaskId:'task-1',initialReviewSeverity:2,finalReviewSeverity:1}).changes.quality,76);
const refs=acceptedQuestionReferences('data'),answers={};for(const q of questionsForState({})){const ref=refs[q.id];answers[q.id]=Array.isArray(ref)?ref[0]:ref}answers.parties='العامل + العميل فقط';const perfectEvidence=['contract','priceSetting','allocation','monitoring','clientQuality','payment','accessDecision'],perfectSort={contract:'dep',priceSetting:'ctrl',allocation:'ctrl',monitoring:'ctrl',clientQuality:'ctrl',payment:'dep',accessDecision:'ctrl'},completedAxes=axes.map(a=>a.id),analysisState={answers,evidence:perfectEvidence,evidenceSort:perfectSort,power:powerTargets.data,powerTouched:completedAxes};assert.equal(powerMapComplete(analysisState),true);const analysis=scoreAnalysis(scenarios.data,analysisState);assert.equal(analysis.score,100);assert.equal(powerAxisCredit(powerTargets.data.price,powerTargets.data.price),1);
console.log('No Boss domain checks passed');
