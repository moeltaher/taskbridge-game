import {test,expect} from '@playwright/test';
async function expectRoute(page,route){await expect(page).toHaveURL(new RegExp(`/${route}/$`))}
async function assertViewportIntegrity(page){const layout=await page.evaluate(()=>{const root=document.documentElement,viewport=root.clientWidth,offenders=[];for(const element of document.querySelectorAll('body *')){const style=getComputedStyle(element);if(style.display==='none'||style.visibility==='hidden')continue;const rect=element.getBoundingClientRect();if(rect.width===0&&rect.height===0)continue;if(rect.left<-2||rect.right>viewport+2){if(style.overflowX==='auto'||style.overflowX==='scroll')continue;offenders.push({tag:element.tagName,left:rect.left,right:rect.right})}}return {clientWidth:viewport,scrollWidth:root.scrollWidth,offenders}});expect(layout.scrollWidth,`horizontal overflow: ${JSON.stringify(layout.offenders.slice(0,5))}`).toBeLessThanOrEqual(layout.clientWidth+2)}
async function capture(page,testInfo,name){await assertViewportIntegrity(page);await page.screenshot({path:testInfo.outputPath(`${name}.png`),fullPage:true,animations:'disabled'})}
async function startScenario(page,name='ليان'){await page.goto('/');await page.getByRole('button',{name:'ابدأ المحاكاة'}).click();const card=page.locator('article.scenario').filter({hasText:name});await card.getByRole('button',{name:'اختيار هذه الحالة'}).click();await page.getByRole('button',{name:'أوافق وأدخل No Boss'}).click();await expectRoute(page,'work')}
async function clickFirstAnswers(page,selector='.sample'){const blocks=page.locator(selector);for(let i=0;i<await blocks.count();i++)await blocks.nth(i).locator('button.choice').first().click()}
async function choosePreferredTranslation(page){const prefs=['A','A','B','A','A','A','B','A'];const state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));for(let i=0;i<state.currentTaskSampleIndexes.length;i++){const sampleIndex=state.currentTaskSampleIndexes[i],choice=prefs[sampleIndex];await page.locator('.sample').nth(i).locator('button.choice').nth(choice==='A'?0:1).click()}}
async function classifyEvidence(page){const docs=page.locator('.doc').filter({has:page.locator('.sort')});for(let i=0;i<await docs.count();i++)await docs.nth(i).locator('.sort').first().click()}
async function fillResearchQuestions(page,expected){const selects=page.locator('select[data-q]');await expect(selects).toHaveCount(expected);for(let i=0;i<expected;i++)await selects.nth(i).selectOption({index:1})}
async function fillPower(page,count){for(let i=0;i<count;i++){const slider=page.locator('fieldset.power-card input[type="range"]').first();await slider.evaluate(el=>{el.value='40';el.dispatchEvent(new Event('input',{bubbles:true}))});if(i<count-1)await page.getByRole('button',{name:'المحور التالي'}).click()}}
async function finishWorkerReview(page){await page.getByRole('button',{name:'متابعة إلى مراجعة جودة العمل'}).click();await expectRoute(page,'dispute');const skipNoReview=page.getByRole('button',{name:'متابعة إلى التسوية'});const skipReview=page.getByRole('button',{name:'المتابعة دون اعتراض'});if(await skipReview.count())await skipReview.click();else await skipNoReview.click();await expectRoute(page,'payment');await page.getByRole('button',{name:'إنهاء التسوية ومراجعة الوصول'}).click();await expectRoute(page,'access')}
async function linkConclusionEvidence(page){const roles=page.locator('select[data-evidence-role]');expect(await roles.count()).toBeGreaterThan(2);await roles.nth(0).selectOption('support');await roles.nth(1).selectOption('support');await roles.nth(2).selectOption('counter')}

test('complete worker-to-researcher journey remains coherent and evidence-backed',async({page},testInfo)=>{test.slow();await startScenario(page,'ليان');await capture(page,testInfo,'04-work-market');await page.getByRole('button',{name:'قبول',exact:true}).first().click();await clickFirstAnswers(page);await page.getByRole('button',{name:'إرسال العمل للمراجعة'}).click();await expect(page.getByText('نتيجة المهمة الأولى')).toBeVisible();await page.getByRole('button',{name:'رؤية أثر الأداء على فرص العمل'}).click();await expectRoute(page,'management');await capture(page,testInfo,'05-management-ranking');await page.getByRole('button',{name:'رؤية العرض الناتج عن هذا التحديث'}).click();await page.getByRole('button',{name:'أرفض هذا العرض'}).click();await page.getByRole('button',{name:'متابعة الوردية'}).click();await page.getByRole('button',{name:/استراحة 3 د/}).click();await expectRoute(page,'risk');await capture(page,testInfo,'06-risk');await finishWorkerReview(page);await capture(page,testInfo,'09-access');await expect(page.getByText('انتهى الفصل الأول: وردية العامل')).toBeVisible();await page.getByRole('button',{name:'ابدأ تحليل الوردية'}).click();await expectRoute(page,'investigation');await capture(page,testInfo,'10-investigation');await page.getByRole('button',{name:'ابدأ تصنيف الأدلة'}).click();await classifyEvidence(page);await page.getByRole('button',{name:'انتقل إلى أسئلة العلاقة'}).click();await fillResearchQuestions(page,6);await page.getByRole('button',{name:'ابن خريطة السلطة'}).click();await expectRoute(page,'power');await capture(page,testInfo,'11-power');await fillPower(page,6);await page.getByRole('button',{name:'اكتب استنتاجك'}).click();await expectRoute(page,'conclusion');await capture(page,testInfo,'12-conclusion');const analysis='تتركز سلطة مؤثرة لدى المنصة والعميل لأنهما يتحكمان في توزيع العمل ومعيار الجودة والوصول إلى الفرص.';await page.locator('#analysis').fill(analysis);await linkConclusionEvidence(page);await page.getByRole('button',{name:'إظهار النتيجة'}).click();await expectRoute(page,'result');await capture(page,testInfo,'13-result');await expect(page.getByText('هدف المقابل')).toBeVisible();await expect(page.getByText('الخلاصة في لمحة')).toBeVisible();await page.getByRole('button',{name:'اربط التجربة بالحقوق'}).click();await expectRoute(page,'rights');await capture(page,testInfo,'14-rights');await expect(page.getByRole('heading',{name:'برز بسبب ما حدث في جولتك'})).toBeVisible();await expect(page.getByRole('heading',{name:'مرتبط بسياق هذه الشخصية'})).toBeVisible();await page.getByRole('button',{name:/الأجر والدخل العادل/}).click();await page.getByRole('button',{name:'إظهار خريطة الحماية'}).click();await expect(page.getByText('خريطة حماية مقترحة للنقاش')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(saved.powerTouched).toHaveLength(6);expect(saved.analysisText).toBe(analysis);expect(saved.conclusionEvidence.length).toBeGreaterThanOrEqual(2);expect(saved.conclusionCounterEvidence.length).toBeGreaterThanOrEqual(1);expect(saved.opportunityRankingDecision).toBeTruthy();expect(saved.marketTime).toBeGreaterThan(0)});

test('excellent real performance can unlock the premium second offer',async({page})=>{await startScenario(page,'مريم');await page.getByRole('button',{name:'قبول',exact:true}).first().click();await choosePreferredTranslation(page);await page.getByRole('button',{name:'إرسال العمل للمراجعة'}).click();let state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(state.completedTasks[0].score).toBeGreaterThanOrEqual(85);await page.getByRole('button',{name:'رؤية أثر الأداء على فرص العمل'}).click();await expect(page.getByText('أداؤك فتح العرض المميز فعلًا.')).toBeVisible();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(state.access).toBeGreaterThanOrEqual(82);expect(state.opportunityRankingDecision.taskScore).toBe(state.completedTasks[0].score);await page.getByRole('button',{name:'رؤية العرض الناتج عن هذا التحديث'}).click();await expect(page.getByText('العرض المميز')).toBeVisible();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(state.secondOffer.premium).toBe(true)});

test('second task is real: unique samples, pay, stress, and quality all update',async({page})=>{await startScenario(page,'مريم');await page.getByRole('button',{name:'قبول',exact:true}).first().click();await choosePreferredTranslation(page);await page.getByRole('button',{name:'إرسال العمل للمراجعة'}).click();await page.getByRole('button',{name:'رؤية أثر الأداء على فرص العمل'}).click();await page.getByRole('button',{name:'رؤية العرض الناتج عن هذا التحديث'}).click();await page.getByRole('button',{name:'أقبل المهمة'}).click();const before=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));const firstIndexes=before.completedTasks[0].sampleIndexes,secondIndexes=before.currentTaskSampleIndexes;expect(firstIndexes.some(i=>secondIndexes.includes(i))).toBe(false);await clickFirstAnswers(page);const preSubmit=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));await page.getByRole('button',{name:'إرسال المهمة الثانية'}).click();const after=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(after.completedTasks).toHaveLength(2);expect(after.grossWorker).toBeGreaterThan(preSubmit.grossWorker);expect(after.stress).toBeGreaterThan(preSubmit.stress);expect(after.completedTasks[1].qualityBefore).toBe(preSubmit.quality);expect(after.quality).toBe(after.completedTasks[1].qualityAfter);await expect(page.getByText('اكتملت المهمة الثانية وأثرت في الحساب.')).toBeVisible()});

test('market exit and offer rejection are separate decisions and market time survives no-work exit',async({page})=>{await startScenario(page,'سامر');await page.getByRole('button',{name:'رفض هذا العرض'}).first().click();let state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(state.rejections).toBe(1);expect(state.marketTime).toBeGreaterThan(0);await page.getByRole('button',{name:'إنهاء الوردية دون قبول عمل'}).click();await expectRoute(page,'access');await expect(page.getByRole('heading',{name:'الحساب نشط مع تحذير'})).toBeVisible();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(state.rejections).toBe(1);expect(state.marketExit).toBe(true);expect(state.marketTime).toBeGreaterThan(0);expect(state.accountOutcome).toBe('warning')});

test('rejecting two offers still produces the stronger rejection-based no-work outcome',async({page})=>{await startScenario(page,'سامر');for(let i=0;i<2;i++)await page.getByRole('button',{name:'رفض هذا العرض'}).first().click();await page.getByRole('button',{name:'إنهاء الوردية دون قبول عمل'}).click();await expectRoute(page,'access');await expect(page.getByRole('heading',{name:'تم تقييد المشروع الحالي'})).toBeVisible();await page.getByRole('button',{name:'ابدأ تحليل الوردية'}).click();await expectRoute(page,'investigation');await expect(page.getByText('هذه وردية انتهت بلا عمل.')).toBeVisible();await page.getByRole('button',{name:'ابدأ تصنيف الأدلة'}).click();await classifyEvidence(page);await page.getByRole('button',{name:'انتقل إلى أسئلة العلاقة'}).click();await fillResearchQuestions(page,4);await page.getByRole('button',{name:'ابن خريطة السلطة'}).click();await fillPower(page,3);const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(saved.noWorkEnding).toBe(true);expect(saved.grossWorker).toBe(0);expect(saved.payment).toBe(null);expect(saved.marketTime).toBeGreaterThan(0);expect(saved.accountOutcome).toBe('project')});

test('standard contract can be declined without fabricating a work rejection',async({page})=>{await page.goto('/');await page.getByRole('button',{name:'ابدأ المحاكاة'}).click();const card=page.locator('article.scenario').filter({hasText:'سامر'});await card.getByRole('button',{name:'اختيار هذه الحالة'}).click();await page.getByRole('button',{name:'لا أوافق على الشروط'}).click();await expect(page.getByText('رفضت شروط الحساب، لذلك لم تدخل سوق المهام.')).toBeVisible();const state=await page.evaluate(()=>JSON.parse(localStorage.getItem('no_boss_state')));expect(state.termsDecision).toBe('declined');expect(state.rejections).toBe(0);expect(state.completedTasks).toHaveLength(0);expect(state.accountOutcome).toBe(null)});
