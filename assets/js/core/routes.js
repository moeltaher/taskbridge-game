export const routes={home:'',scenario:'scenario',onboarding:'onboarding',work:'work',management:'management',risk:'risk',dispute:'dispute',payment:'payment',access:'access',investigation:'investigation',power:'power',conclusion:'conclusion',result:'result',rights:'rights'};
export const progress={home:[0,'قبل الوردية'],scenario:[4,'اختيار الحالة'],onboarding:[9,'العقد'],work:[28,'تنفيذ العمل'],management:[48,'الإدارة الخوارزمية'],risk:[58,'المخاطر'],dispute:[66,'نزاع الجودة'],payment:[74,'توزيع القيمة'],access:[81,'الوصول إلى العمل'],investigation:[91,'التحقيق'],power:[97,'خريطة السلطة'],conclusion:[99,'الاستنتاج'],result:[100,'النتيجة'],rights:[100,'خريطة الحقوق']};
export function projectBase(){const parts=location.pathname.split('/').filter(Boolean);const known=new Set(Object.values(routes).filter(Boolean));if(parts.length&&known.has(parts.at(-1)))parts.pop();return '/'+(parts.length?parts.join('/')+'/':'');}
export function href(page){const slug=routes[page]??'';return projectBase()+(slug?slug+'/':'');}
export function pageFromPath(){const parts=location.pathname.split('/').filter(Boolean);const last=parts.at(-1)||'';for(const [k,v] of Object.entries(routes))if(v===last)return k;return 'home';}
