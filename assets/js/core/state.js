import {axes} from '../data/parties.js';
import {APP_VERSION,TIME_MODEL_VERSION} from './config.js';
import {pageForStage as routePageForStage,stageForPage as routeStageForPage,isPublicPage} from './routes.js';
import {saveState,loadState,clearState,stateStorageMode,latestStateRevision} from './storage.js';

const writerId=globalThis.crypto?.randomUUID?.()||`tab-${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`;
let lastPersistence={ok:true,status:'persistent',persistent:true,session:false},warnedPersistence=false,warnedSession=false,revisionCounter=0;
function persist(){revisionCounter=Math.max(revisionCounter,Number(state.storageRevision)||0,latestStateRevision())+1;state.storageRevision=revisionCounter;state.storageWriterId=writerId;lastPersistence=saveState(state);if(lastPersistence.status==='session'&&!warnedSession){warnedSession=true;globalThis.alert?.('تعذر الحفظ الدائم، لكن تقدم الجولة محفوظ مؤقتًا داخل هذا التبويب. لا تغلق التبويب إذا أردت الاحتفاظ بالتقدم.')}if(lastPersistence.status==='failed'&&!warnedPersistence){warnedPersistence=true;globalThis.alert?.('تعذر حفظ تقدم الجولة في هذا المتصفح. لا تغلق الصفحة أو تنتقل منها قبل السماح بالتخزين؛ قد تضيع التغييرات غير المحفوظة.')}return lastPersistence}
function applyChanges(changes){const keys=Object.keys(changes||{});if(!keys.length)return false;Object.assign(state,changes);if('breakTime'in changes||'extraWorkTime'in changes)state.unpaidTime=Number(state.breakTime||0)+Number(state.extraWorkTime||0);return true}
function appendLog({title,text}){const h=9+Math.floor(state.time/60),m=String(state.time%60).padStart(2,'0');state.log.push({time:`${String(h).padStart(2,'0')}:${m}`,title,text})}
export function freshState(){const power={};axes.forEach(a=>power[a.id]={worker:25,platform:25,client:25,mediator:25});return {version:APP_VERSION,storageRevision:0,storageWriterId:null,stage:0,scenarioKey:null,status:'غير نشط',grossWorker:0,clientPaid:0,time:0,paidTime:0,unpaidTime:0,extraWorkTime:0,breakTime:0,timeModelVersion:TIME_MODEL_VERSION,timeBucketsInferred:false,quality:92,qualityAfterFirstTask:null,firstTaskStress:null,initialStress:22,acceptance:100,acceptedOffers:0,offerDecisions:0,access:72,stress:22,jobsDone:0,rejections:0,rejectedJobs:[],selectedJob:null,workAnswers:[],workScore:0,workStep:'market',secondOffer:null,managementStep:'ranking',rankingBeforeAccess:null,rushAccepted:null,offerDecisionResult:null,tookBreak:null,monitorDecision:null,riskEvent:null,riskApplied:false,appealed:null,appealCostApplied:false,appealCost:null,disputeSeverity:0,hold:0,disputeFinalized:false,qualityBeforeDispute:null,accessBeforeDispute:null,disputeResult:null,payment:null,accountOutcome:null,accessDecision:null,evidence:[],log:[],evidenceSort:{},answers:{},investigationStep:'case',power,powerDraft:{},powerTouched:[],powerEdited:[],analysisText:'',conclusionEvidence:[],selectedRights:[],resultData:null,realStartedAt:null,realFinishedAt:null,currentPage:'home',checkpoints:[]};}
function inferStage(saved){if(Number.isInteger(saved?.stage))return saved.stage;const p=saved?.currentPage||'home';return routeStageForPage(p)??(saved?.scenarioKey?1:0)}
function migrateState(saved){const base=freshState();if(!saved||saved.version!==APP_VERSION)return base;const hadPowerEdited=Array.isArray(saved.powerEdited),stage=inferStage(saved),next={...base,...saved,version:APP_VERSION,stage,storageRevision:Math.max(0,Number(saved.storageRevision)||0)},legacyOutside=Number(saved.unpaidTime||0);if(saved.timeModelVersion!==TIME_MODEL_VERSION||saved.extraWorkTime===undefined||saved.breakTime===undefined){const inferredBreak=saved.breakTime!==undefined?Number(saved.breakTime||0):saved.monitorDecision?.tookBreak===true?Number(saved.monitorDecision.breakDelta??saved.monitorDecision.unpaidDelta??1):saved.tookBreak===true?1:0;const inferredExtra=saved.extraWorkTime!==undefined?Number(saved.extraWorkTime||0):Math.max(0,legacyOutside-inferredBreak);next.breakTime=inferredBreak;next.extraWorkTime=inferredExtra;next.unpaidTime=inferredBreak+inferredExtra;next.timeModelVersion=TIME_MODEL_VERSION;next.timeBucketsInferred=legacyOutside>0||inferredBreak>0||inferredExtra>0}else{next.breakTime=Number(next.breakTime||0);next.extraWorkTime=Number(next.extraWorkTime||0);next.unpaidTime=next.breakTime+next.extraWorkTime}if(!next.powerDraft||typeof next.powerDraft!=='object'||Array.isArray(next.powerDraft))next.powerDraft={};if(!Array.isArray(next.powerTouched))next.powerTouched=[];if(!hadPowerEdited){next.powerEdited=[];next.powerTouched=[];next.powerDraft={}}else if(!Array.isArray(next.powerEdited))next.powerEdited=[];return next}
const saved=loadState();let state=migrateState(saved);revisionCounter=Number(state.storageRevision)||0;const initialMode=stateStorageMode();lastPersistence=initialMode==='session'?{ok:true,status:'session',persistent:false,session:true}:{ok:true,status:'persistent',persistent:true,session:false};
export function getState(){return state}
export function persistenceStatus(){return lastPersistence}
export function pageForStage(stage=state.stage){return routePageForStage(stage)}
export function resumePage(s=state){const current=s?.currentPage,stage=Number(s?.stage||0);if(s?.scenarioKey&&current&&!isPublicPage(current)&&routeStageForPage(current)===stage)return current;if(s?.scenarioKey&&stage>0)return pageForStage(stage);return current&&current!=='home'?current:'scenario'}
export function setState(next){state=migrateState(next);revisionCounter=Math.max(revisionCounter,Number(state.storageRevision)||0);persist();return state}
export function commit({changes={},evidence=[],log=null,recalculateAcceptance=false}={}){let dirty=applyChanges(changes);for(const id of evidence){if(!state.evidence.includes(id)){state.evidence.push(id);dirty=true}}if(recalculateAcceptance){const acceptance=state.offerDecisions?Math.round(state.acceptedOffers/state.offerDecisions*100):100;if(acceptance!==state.acceptance){state.acceptance=acceptance;dirty=true}}if(log){appendLog(log);dirty=true}if(dirty)persist();return state}
export function patch(changes){return commit({changes})}
export function reset(){state=freshState();clearState();warnedPersistence=false;warnedSession=false;revisionCounter=0;persist();return state}
export function checkpoint(targetPage,{persistNow=true}={}){const snap=structuredClone(state);snap.checkpoints=[];state.checkpoints.push({page:state.currentPage,snapshot:snap,target:targetPage});if(state.checkpoints.length>24)state.checkpoints.shift();if(persistNow)persist();return state}
export function enterPage(page,{record=true}={}){if(state.currentPage===page)return state;if(record)checkpoint(page,{persistNow:false});state.currentPage=page;persist();return state}
export function undoCheckpoint(){const item=state.checkpoints.pop();if(!item)return null;const keep=state.checkpoints;state=migrateState(item.snapshot);state.checkpoints=keep;state.stage=routeStageForPage(item.page);state.currentPage=item.page;persist();return item.page}
export function consumeCheckpointTo(expectedPage){const item=state.checkpoints.at(-1);if(item?.page!==expectedPage)return null;state.checkpoints.pop();state.currentPage=expectedPage;state.stage=routeStageForPage(expectedPage);persist();return expectedPage}
export function timeBreakdown(s=state){return {taskTime:Number(s.paidTime||0),extraWorkTime:Number(s.extraWorkTime||0),breakTime:Number(s.breakTime||0),totalTime:Number(s.time||0)}}
export function wellbeingLabel(v){return v>=70?'عبء مرتفع':v>=40?'عبء متوسط':'عبء منخفض'}
export const clamp=(v,min,max)=>Math.max(min,Math.min(max,v));
export const money=v=>'$'+Number(v||0).toFixed(2);
