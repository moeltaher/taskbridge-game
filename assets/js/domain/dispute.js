import {samples} from '../data/samples.js';
import {dataAnswerCredit} from './work.js';
export const APPEAL_TIME_MINUTES=2;
export const APPEAL_STRESS=4;
export const appealGrounds={guideline:'تفسير الإرشادات أو غموض المعيار',context:'سياق العينة أو معناها',technical:'مشكلة تقنية أو عرض غير دقيق'};
const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));
export function reviewSeverityFromScore(score){return score>=85?0:score>=60?1:2}
export function reviewedSeverity(initialSeverity,appealAccepted=true){return appealAccepted&&initialSeverity>0?initialSeverity-1:initialSeverity}
export function selectReviewTask(state){const tasks=state.completedTasks||[];if(!tasks.length)return null;return [...tasks].sort((a,b)=>a.score-b.score||String(b.id).localeCompare(String(a.id)))[0]}
function groundMatches(scenario,ground,{reviewableErrors=0,hardErrors=0,technicalIssues=0}={}){if(!ground)return false;if(scenario.type==='data'){if(ground==='technical')return technicalIssues>0;if(ground==='guideline')return reviewableErrors>0&&hardErrors<=1;return false}if(ground==='technical')return false;if(scenario.type==='moderation')return reviewableErrors>0&&hardErrors<=1&&(ground==='context'||ground==='guideline');if(scenario.type==='ai')return reviewableErrors>0&&hardErrors<=1&&ground==='guideline';if(scenario.type==='translation')return reviewableErrors>0&&hardErrors<=1&&(ground==='guideline'||ground==='context');return false}
function dataReviewProfile(task,state){let reviewableErrors=0,hardErrors=0;task.answers.forEach((answer,i)=>{const credit=dataAnswerCredit(task.sampleIndexes[i],answer);if(credit>=.55&&credit<.85)reviewableErrors++;else if(credit<.55)hardErrors++});return {reviewableErrors,hardErrors,technicalIssues:task.technicalIssue===true||state.riskEvent?.technicalIssue===true?1:0}}
export function reviewAppeal(scenario,state){const task=(state.completedTasks||[]).find(t=>t.id===state.reviewTaskId)||selectReviewTask(state),initial=Number(state.initialReviewSeverity??0),ground=state.appealGround;if(!task||initial<=0)return {accepted:false,finalSeverity:0,reason:'لا توجد مراجعة سلبية تستدعي تخفيفًا.',reviewableErrors:0,hardErrors:0,ground};if(!ground)return {accepted:false,finalSeverity:initial,reason:'لم يُحدد سبب للاعتراض، لذلك لا توجد مسألة محددة لإعادة الفحص.',reviewableErrors:0,hardErrors:0,ground};if(scenario.type==='data'){const profile=dataReviewProfile(task,state),accepted=groundMatches(scenario,ground,profile);return {accepted,finalSeverity:reviewedSeverity(initial,accepted),...profile,ground,reason:accepted&&ground==='technical'?'توجد واقعة تقنية مسجلة مرتبطة بعرض/مزامنة الإجابة، لذلك أعادت المراجعة فحص المهمة وخفضت شدة القرار درجة واحدة.':accepted?`وجدت المراجعة ${profile.reviewableErrors} انحرافًا حدّيًا يمكن مناقشته وفق قاعدة الترميز، وكان سبب الاعتراض (${appealGrounds[ground]}) مرتبطًا به؛ لذلك خُفض القرار درجة واحدة.`:ground==='technical'&&profile.technicalIssues===0?'لا تسجل الجولة عطلًا تقنيًا مرتبطًا بالمهمة، لذلك لا يمكن قبول سبب تقني لمجرد انخفاض النتيجة.':`الانحرافات المسجلة لا تدعم سبب الاعتراض (${appealGrounds[ground]}) بما يكفي لتغيير القرار.`}}let reviewableErrors=0,hardErrors=0;task.answers.forEach((answer,i)=>{const sample=samples[scenario.type][task.sampleIndexes[i]];if(sample?.acceptable?.includes(answer))return;if(sample?.reviewable?.includes(answer))reviewableErrors++;else hardErrors++});const accepted=groundMatches(scenario,ground,{reviewableErrors,hardErrors});return {accepted,finalSeverity:reviewedSeverity(initial,accepted),reviewableErrors,hardErrors,technicalIssues:0,ground,reason:accepted?`وجدت المراجعة ${reviewableErrors} إجابة قابلة للدفاع، وكان سبب الاعتراض (${appealGrounds[ground]}) ذا صلة بنوع الخلاف؛ لذلك خُفضت شدة القرار درجة واحدة.`:`وجدت المراجعة ${reviewableErrors} إجابة قابلة للدفاع و${hardErrors} خطأ واضحًا، لكن سبب الاعتراض (${appealGrounds[ground]}) لم يكن كافيًا لتغيير النتيجة في هذه الحالة.`}}
export function disputeConsequences(state,severity){const task=(state.completedTasks||[]).find(t=>t.id===state.reviewTaskId),disputedPay=Number(task?.pay??state.selectedJob?.pay??0);let hold=0,penalty=0;if(severity===1){hold=Math.min(.45,disputedPay*.10);penalty=4}else if(severity>=2){hold=Math.min(.9,disputedPay*.18);penalty=9}return {hold,penalty,disputedPay}}
export function applyDisputeOutcome(state){const qualityBefore=state.qualityBeforeDispute??state.quality,initialSeverity=Number(state.initialReviewSeverity??0),finalSeverity=Number(state.finalReviewSeverity??initialSeverity),consequences=disputeConsequences(state,finalSeverity),qualityAfter=clamp(qualityBefore-consequences.penalty,0,100);return {consequences,initialSeverity,finalSeverity,qualityBefore,qualityAfter,changes:{hold:consequences.hold,quality:qualityAfter,initialReviewSeverity:initialSeverity,finalReviewSeverity:finalSeverity,qualityBeforeDispute:qualityBefore,disputeFinalized:true,stage:6,status:'قيد التسوية',payment:null}}}
export function appealCostChanges(state){return {appealCost:{minutes:APPEAL_TIME_MINUTES,stress:APPEAL_STRESS},time:state.time+APPEAL_TIME_MINUTES,extraWorkTime:Number(state.extraWorkTime||0)+APPEAL_TIME_MINUTES,stress:clamp(state.stress+APPEAL_STRESS,0,100)}}
export function publishedTranslationText(sample){const key=String(sample?.preferred||'A').toLowerCase();return sample?.[key]??sample?.a??''}
