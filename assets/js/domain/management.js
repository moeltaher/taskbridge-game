import {acceptanceRate,scoreWork,qualityAfterTask,taskRecord,nextSampleIndexes,dataReviewMetrics} from './work.js';
import {secondOfferForAccess} from './offers.js';

export const BREAK_MINUTES=3;
export const BREAK_STRESS_REDUCTION=8;
const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));

export function computeManagedAccess(scenario,state){const score=Number(state.completedTasks?.[0]?.score??state.workScore??85),performanceDelta=Math.round((score-85)*.4),acceptanceDelta=Math.round((Number(state.acceptance??100)-100)*.12);return clamp(Number(scenario.initial.access)+performanceDelta+acceptanceDelta,35,95)}
export function buildSecondOffer(scenario,access){return secondOfferForAccess(scenario,access)}
export function secondTaskStressDelta(offer){return Number(offer?.stress||0)}
export function secondOfferDecision(state,accepted){const offer=state.secondOffer,before={acceptance:state.acceptance,access:state.access,stress:state.stress};const offerDecisions=state.offerDecisions+1,acceptedOffers=state.acceptedOffers+(accepted?1:0),rejections=state.rejections+(accepted?0:1),acceptance=acceptanceRate(acceptedOffers,offerDecisions);return {offerDecisions,acceptedOffers,rejections,acceptance,result:{accepted,completed:false,title:offer.title,pay:offer.pay,duration:offer.duration,beforeAcceptance:before.acceptance,afterAcceptance:acceptance,beforeAccess:before.access,afterAccess:before.access,beforeStress:before.stress,afterStress:before.stress}}}
export function prepareSecondTask(state){return nextSampleIndexes(state,state.secondOffer.sampleCount)}
export function completeSecondTask(scenario,state){const offer=state.secondOffer,decision=state.offerDecisionResult,indexes=state.currentTaskSampleIndexes||nextSampleIndexes(state,offer.sampleCount),score=scoreWork(scenario,state.secondTaskAnswers,{sampleIndexes:indexes}),qualityBefore=state.quality,quality=qualityAfterTask(qualityBefore,score),stressDelta=secondTaskStressDelta(offer),afterStress=clamp(state.stress+stressDelta,0,100),record=taskRecord({id:'task-2',job:offer,sampleIndexes:indexes,answers:state.secondTaskAnswers,score,qualityBefore,qualityAfter:quality,stressDelta,reviewMetrics:scenario.type==='data'?dataReviewMetrics(state.secondTaskAnswers,indexes):null});return {stressDelta,score,record,changes:{grossWorker:state.grossWorker+offer.pay,clientPaid:state.clientPaid+offer.clientValue,time:state.time+offer.duration,paidTime:state.paidTime+offer.duration,stress:afterStress,quality,secondTaskScore:score,completedTasks:[...(state.completedTasks||[]),record],sampleCursor:Number(state.sampleCursor||0)+indexes.length,offerDecisionResult:{...decision,completed:true,afterStress,score,qualityBefore,qualityAfter:quality},managementStep:'offerResult',status:'اكتملت المهمة الثانية'}}}
export function monitorDecision(state,takeBreak){const before=state.stress,after=takeBreak?clamp(before-BREAK_STRESS_REDUCTION,0,100):before;return {tookBreak:takeBreak,stressBefore:before,stressAfter:after,stressDelta:after-before,timeDelta:takeBreak?BREAK_MINUTES:0,breakDelta:takeBreak?BREAK_MINUTES:0}}
