import {scenarios} from '../data/scenarios.js';
import {getState,patch,commit,money,timeBreakdown} from '../core/state.js';
import {href} from '../core/routes.js';
import {refreshStats} from '../core/ui.js';
import {riskTransition} from '../domain/risk.js';

function ensureRisk(scenario){
 const transition=riskTransition(scenario,getState());
 if(transition.changes){
  if(transition.isNew){commit({changes:transition.changes,evidence:['risk'],log:{title:'حدث موقف إضافي مرتبط بالعمل',text:`${transition.event.title}: أضيفت ${transition.event.minutes} دقيقة إلى الوقت الإضافي المرتبط بالعمل.`}})}
  else patch(transition.changes);
 }
 return transition.event;
}

function previousDecisionHTML(state){const decision=state.monitorDecision;if(!decision)return '<div class="notice"><b>قرار الاستراحة السابق غير محفوظ.</b></div>';if(decision.tookBreak)return `<div class="notice good"><b>قبل الحدث: أخذت استراحة ${decision.breakDelta||0} د.</b><div style="margin-top:5px">سجلت ${decision.breakDelta||0} د كاستراحة اختيارية وانخفض الضغط من <b>${decision.stressBefore}/100</b> إلى <b>${decision.stressAfter}/100</b>. الحدث أدناه مستقل عن قرار الاستراحة.</div></div>`;return `<div class="notice info"><b>قبل الحدث: واصلت دون استراحة.</b><div style="margin-top:5px">لم تضف مدة استراحة، وبقي الضغط عند <b>${decision.stressAfter}/100</b> بسبب هذا القرار وحده. الحدث أدناه مستقل عنه.</div></div>`}

export function render(root){const initial=getState(),scenario=scenarios[initial.scenarioKey],risk=ensureRisk(scenario),state=getState(),time=timeBreakdown(state);root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">9</span><div><b>ظهر موقف جديد بعد العمل المنجز</b><small>لا تفترض الصفحة وجود مهمة مفتوحة لم ترها؛ الحدث مرتبط بمهمة أو تسليم سبق أن حدث في الجولة.</small></div></div>${previousDecisionHTML(state)}<div class="notice info"><b>${scenario.name} · ${scenario.role}</b><div style="margin-top:5px">نوع الموقف: <b>${risk.kind}</b>.</div></div><h2>${risk.icon} ${risk.title}</h2><div class="timeline"><div class="tl"><small>ما سبق</small><b>أنجزت عملًا في مشروع ${scenario.client}</b><div class="muted small">المهمة أو التسليم المشار إليه موجود بالفعل في سجل الجولة.</div></div><div class="tl"><small>الحدث الجديد الآن</small><b>${risk.title}</b><div class="muted small">${risk.cause}</div></div><div class="tl"><small>أثر الحدث</small><b>+${risk.minutes} د وقت إضافي مرتبط بالعمل · +${risk.stress} ضغط</b><div class="muted small">${risk.consequence}</div></div></div><div class="grid-4"><div class="metric"><small>وقت المهمات المحتسب</small><b>${time.taskTime} د</b></div><div class="metric"><small>الوقت الإضافي المرتبط بالعمل بلا مقابل مستقل</small><b>${time.extraWorkTime} د</b></div><div class="metric"><small>الاستراحة الاختيارية</small><b>${time.breakTime} د</b></div><div class="metric"><small>إجمالي الوردية</small><b>${time.totalTime} د</b></div></div><div class="notice"><b>لماذا لا يوجد مقابل مستقل لهذه الدقائق؟</b><div style="margin-top:5px">المقابل في المحاكاة يسجل للمهمات ذات السعر التي قبلتها وأكملتها. الحدث أضاف عملاً أو انتظارًا أو تعافيًا مرتبطًا بتسليم قائم ولم ينشئ مهمة جديدة بسعر مستقل.</div></div><div class="notice"><b>تكاليف التشغيل</b><div style="margin-top:5px">الإنترنت ${money(scenario.costs.internet)} · الكهرباء ${money(scenario.costs.electricity)} · استهلاك الجهاز ${money(scenario.costs.device)}.</div></div><div class="actions"><button class="btn" id="next">متابعة إلى مراجعة جودة العمل</button></div></div>`;refreshStats();document.getElementById('next').onclick=()=>{patch({stage:5,status:'مراجعة جودة العمل'});location.href=href('dispute')}}
