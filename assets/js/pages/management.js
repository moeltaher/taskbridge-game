import {scenarios} from '../data/scenarios.js';
import {getState,patch,commit,money,timeBreakdown,wellbeingLabel} from '../core/state.js';
import {href} from '../core/routes.js';
import {refreshStats} from '../core/ui.js';
import {SECOND_TASK_ACCESS_BONUS,BREAK_MINUTES,BREAK_STRESS_REDUCTION,secondTaskStressDelta,computeManagedAccess,buildSecondOffer,secondOfferDecision,completeSecondTask,monitorDecision} from '../domain/management.js';

function ranking(root,sc,s){const access=computeManagedAccess(sc,s);let before=s.rankingBeforeAccess,changes={};if(before===undefined||before===null){before=s.access;changes.rankingBeforeAccess=before}if(s.access!==access){changes.access=access;commit({changes,evidence:['allocation','opacity'],log:{title:'أعيد حساب الوصول إلى المهام',text:`بعد نتيجة المهمة الأولى، أعادت No Boss حساب مؤشر الوصول من ${before}/100 إلى ${access}/100.`}})}else if(Object.keys(changes).length)patch(changes);const change=access-before,changeText=change>0?`ارتفع ${change} نقاط`:change<0?`انخفض ${Math.abs(change)} نقاط`:'لم يتغير';root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">6</span><div><b>تم تحديث ترتيب حسابك</b><small>تعتمد هذه المرحلة على جودة الحساب ومعدل قبول العروض. سجل الرفض لا يُحسب مرة ثانية هنا؛ سيظهر مستقلًا لاحقًا في قرار الوصول النهائي.</small></div></div><h2>تحديث الوصول إلى العمل</h2><div class="grid-4"><div class="metric"><small>الوصول قبل التحديث</small><b>${before}/100</b></div><div class="metric"><small>الجودة</small><b>${s.quality}%</b></div><div class="metric"><small>معدل القبول</small><b>${s.offerDecisions?s.acceptance+'%':'لا يوجد سجل بعد'}</b></div><div class="metric"><small>الوصول بعد التحديث</small><b>${access}/100</b></div></div><div class="notice info"><b>النتيجة:</b> ${changeText}. لا تطبق هذه المعادلة خصمًا إضافيًا لمجرد وجود سجل رفض؛ أثر الرفض الموجود هنا يمر عبر معدل القبول فقط.</div><div class="actions"><button class="btn" id="nextOffer">عرض مهمة ثانية</button></div></div>`;document.getElementById('nextOffer').onclick=()=>{const offer=buildSecondOffer(sc,getState().access);patch({secondOffer:offer,managementStep:'offer',status:'وصل عرض مهمة ثانية'});render(root)}}
function offer(root,sc,s){const current=s.secondOffer;root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">7</span><div><b>عرض مهمة ثانية من ${sc.client}</b><small>القبول يعني الالتزام بالمهمة أولًا؛ لن تضاف مستحقاتها أو مدتها إلا بعد إتمامها في الخطوة التالية.</small></div></div><div class="job"><span class="pill">${current.premium?'عرض مميز':'عرض إضافي'}</span><h2>${current.title}</h2><div class="price">${money(current.pay)} USD</div><p>${current.duration} دقيقة متوقعة</p></div><div class="actions"><button class="btn good" id="yes">أقبل المهمة</button><button class="btn secondary" id="no">أرفض وأستمر</button></div></div>`;document.getElementById('yes').onclick=()=>decide(true,root);document.getElementById('no').onclick=()=>decide(false,root)}
function decide(accepted,root){const state=getState(),offer=state.secondOffer,outcome=secondOfferDecision(state,accepted);if(accepted){commit({changes:{offerDecisions:outcome.offerDecisions,acceptedOffers:outcome.acceptedOffers,acceptance:outcome.acceptance,rushAccepted:true,offerDecisionResult:outcome.result,managementStep:'secondTask',status:'ينفذ المهمة الثانية'},log:{title:'قبلت العرض الثاني',text:`قبلت ${offer.title}. لم تُضف المستحقات بعد؛ يجب إتمام المهمة أولًا.`}})}else{commit({changes:{offerDecisions:outcome.offerDecisions,acceptedOffers:outcome.acceptedOffers,acceptance:outcome.acceptance,rushAccepted:false,rejections:outcome.rejections,offerDecisionResult:outcome.result,managementStep:'offerResult',status:'رفض المهمة الثانية'},evidence:['decline'],log:{title:'رفضت العرض الثاني',text:`رفضت ${offer.title}. لم يُضف وقت أو مقابل ولم يُطبق خصم وصول مباشر؛ سُجل الرفض مرة واحدة وسيظهر في قرار الوصول النهائي.`}})}refreshStats();render(root)}
function secondTask(root,sc,s){const offer=s.secondOffer,stressDelta=secondTaskStressDelta(offer);root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">7</span><div><b>أكمل المهمة التي قبلتها</b><small>هذه خطوة تنفيذ مختصرة؛ لا تعتبر المهمة مكتملة بمجرد القبول.</small></div></div><h2>${offer.title}</h2><div class="notice info">لعدم تكرار تمرين المهمة الأولى بالكامل، تمثل هذه الشاشة تنفيذ دفعة مماثلة من مشروع ${sc.client}. عند الإتمام فقط ستضاف ${offer.duration} دقيقة إلى وقت المهمات المحتسب و${money(offer.pay)} إلى مستحقاتك.</div><div class="notice"><b>افتراض المحاكاة لهذه الدفعة:</b><div style="margin-top:5px">إتمامها يرفع مؤشر العبء/الضغط التدريبي بنحو <b>${stressDelta}</b> نقطة، ويضيف <b>${SECOND_TASK_ACCESS_BONUS}</b> نقاط إلى مؤشر الوصول المرحلي بوصفها نتيجة افتراضية لإتمام دفعة إضافية. العبء النهائي سيظهر كحصيلة مستقلة في صفحة النتيجة ولا يعاقب الأجر سرًا.</div></div><div class="actions"><button class="btn" id="completeSecond">إتمام المهمة الثانية وإرسالها</button></div></div>`;document.getElementById('completeSecond').onclick=()=>completeSecond(root)}
function completeSecond(root){const state=getState(),decision=state.offerDecisionResult;if(!decision?.accepted||decision.completed){patch({managementStep:'offerResult'});render(root);return}const outcome=completeSecondTask(state);commit({changes:outcome.changes,log:{title:'أكملت المهمة الثانية',text:`أضيف ${money(state.secondOffer.pay)} و${state.secondOffer.duration} دقيقة إلى وقت المهمات المحتسب، وارتفع عبء المحاكاة ${outcome.stressDelta} نقطة.`}});refreshStats();render(root)}
function offerResult(root,sc,s){const decision=s.offerDecisionResult||{},accepted=decision.accepted===true;root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">7</span><div><b>${accepted?'أكملت المهمة الثانية':'رفضت المهمة الثانية'}</b><small>هذه نتيجة القرار والتنفيذ المسجلين فعليًا في الجولة.</small></div></div><div class="grid-4"><div class="metric"><small>المقابل المضاف</small><b>${accepted?`+${money(decision.pay)}`:'+$0.00'}</b></div><div class="metric"><small>وقت المهمات المضاف</small><b>+${accepted?decision.duration:0} د</b></div><div class="metric"><small>الوصول المرحلي</small><b>${decision.beforeAccess} ← ${decision.afterAccess}</b></div><div class="metric"><small>${accepted?'العبء/الضغط':'معدل القبول'}</small><b>${accepted?`${decision.beforeStress} ← ${decision.afterStress}`:`${decision.beforeAcceptance}% ← ${decision.afterAcceptance}%`}</b></div></div><div class="notice ${accepted?'good':'info'}">${accepted?'المهمة أضافت دخلًا ووقتًا، وفي المقابل رفعت عبء الوردية. ستظهر هذه المفاضلة في النتيجة النهائية.':'لم يزد الوقت أو الدخل، ولم يُطبق خصم وصول مباشر إضافي؛ سُجل قرار الرفض فقط في معدل القبول وسجل الرفض.'}</div><div class="actions"><button class="btn" id="toMonitor">متابعة الوردية</button></div></div>`;document.getElementById('toMonitor').onclick=()=>{commit({changes:{managementStep:'monitor',status:'متابعة مؤشرات الوردية'},evidence:['monitoring']});render(root)}}
function monitoringCopy(sc){if(sc.monitoring==='light')return {title:'متابعة محدودة للتسليم',detail:'تسجل No Boss وقت التسليم وسجل التعديلات.'};if(sc.monitoring==='intensive')return {title:'متابعة مكثفة أثناء المراجعة',detail:'تسجل No Boss سرعة القرار وفترات الخمول وبعض مؤشرات النشاط.'};if(sc.monitoring==='timing')return {title:'متابعة الزمن والتفاعل',detail:'تسجل No Boss زمن المهمة وتبديل التبويبات وتعديلات الإجابة.'};return {title:'متابعة النشاط أثناء المهمة',detail:'تسجل No Boss فترات النشاط والخمول وتغييرات النافذة.'}}
function previousOfferHTML(s){const decision=s.offerDecisionResult;if(!decision)return'';return decision.accepted?`<div class="notice good"><b>المهمة السابقة:</b> قبلت ثم أكملت ${decision.title}؛ أضيف ${money(decision.pay)} و${decision.duration} دقيقة إلى وقت المهمات المحتسب.</div>`:`<div class="notice info"><b>المهمة السابقة:</b> رفضت ${decision.title}؛ لم يضف القرار وقت مهمة أو مقابلًا.</div>`}
function monitor(root,sc,s){const monitoring=monitoringCopy(sc);root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">8</span><div><b>No Boss تتابع بعض مؤشرات العمل</b><small>هذه المؤشرات قد تدخل في ملف الأداء وترتيب الوصول.</small></div></div>${previousOfferHTML(s)}<h2>ما الذي تراقبه المنصة؟</h2><div class="notice info"><b>${monitoring.title}</b><div style="margin-top:5px">${monitoring.detail}</div></div><div class="grid-2"><div class="card"><small>جودة الحساب الحالية</small><h3>${s.quality}%</h3></div><div class="card"><small>عبء الوردية في المحاكاة</small><h3>${s.stress}/100 · ${wellbeingLabel(s.stress)}</h3><p class="small muted">مؤشر تدريبي مستقل سيظهر في النتيجة النهائية، وليس بيانات نفسية تجمعها المنصة.</p></div></div><div class="notice"><b>قرار الاستراحة:</b> الاستراحة تضيف ${BREAK_MINUTES} د إلى إجمالي زمن الوردية، وتخفض عبء المحاكاة ${BREAK_STRESS_REDUCTION} نقاط. الاستمرار لا يضيف وقت استراحة ولا يخفض العبء. لذلك يوجد هنا تبادل واضح بين الوقت والرفاه التدريبي.</div><div class="actions"><button class="btn secondary" id="break">استراحة ${BREAK_MINUTES} د · عبء -${BREAK_STRESS_REDUCTION}</button><button class="btn" id="continue">أستمر دون استراحة</button></div></div>`;document.getElementById('break').onclick=()=>finishMonitor(true,root);document.getElementById('continue').onclick=()=>finishMonitor(false,root)}
function finishMonitor(takeBreak,root){const state=getState(),decision=monitorDecision(state,takeBreak),changes={tookBreak:takeBreak,stress:decision.stressAfter,monitorDecision:decision,managementStep:'monitorResult',status:takeBreak?'استراحة قصيرة':'واصل العمل دون استراحة'};if(takeBreak){changes.time=state.time+decision.timeDelta;changes.breakTime=Number(state.breakTime||0)+decision.breakDelta}commit({changes,log:takeBreak?{title:'أخذت استراحة قصيرة',text:`سُجلت ${decision.breakDelta} د استراحة اختيارية وانخفض عبء الوردية ${Math.abs(decision.stressDelta)} نقاط.`}:{title:'واصلت دون استراحة',text:'لم تُضف مدة استراحة ولم ينخفض عبء الوردية بسبب هذا القرار.'}});refreshStats();render(root)}
function monitorResult(root,sc,s){const decision=s.monitorDecision||{},takeBreak=decision.tookBreak===true,time=timeBreakdown(s);root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">8</span><div><b>${takeBreak?'أخذت استراحة قبل متابعة الوردية':'واصلت العمل دون استراحة'}</b><small>سيظهر أثر هذا القرار في حصيلة الوقت وعبء الوردية في النتيجة النهائية.</small></div></div><div class="grid-3"><div class="metric"><small>وقت المهمات المحتسب</small><b>${time.taskTime} د</b></div><div class="metric"><small>استراحة اختيارية مضافة</small><b>+${decision.breakDelta||0} د</b></div><div class="metric"><small>تغير عبء الوردية</small><b>${decision.stressDelta>=0?'+':''}${decision.stressDelta||0}</b></div></div><div class="actions"><button class="btn" id="toRisk">متابعة الوردية</button></div></div>`;document.getElementById('toRisk').onclick=()=>{patch({stage:4,status:'عاد إلى العمل'});location.href=href('risk')}}
export function render(root){const state=getState(),scenario=scenarios[state.scenarioKey];if(state.managementStep==='offer')offer(root,scenario,state);else if(state.managementStep==='secondTask')secondTask(root,scenario,state);else if(state.managementStep==='offerResult')offerResult(root,scenario,state);else if(state.managementStep==='monitor')monitor(root,scenario,state);else if(state.managementStep==='monitorResult')monitorResult(root,scenario,state);else ranking(root,scenario,state)}
