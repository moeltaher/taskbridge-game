import {scenarios} from '../data/scenarios.js';
import {getState,patch} from '../core/state.js';
import {href} from '../core/routes.js';
import {escapeHTML} from '../core/html.js';
import {evidenceFor} from '../domain/evidence.js';
import {powerMapComplete,scoreAnalysis,analysisAxes} from '../domain/analysis.js';

const dimensionLabel={contract:'العقد',price:'السعر',allocation:'توزيع العمل',monitoring:'المراقبة',risk:'المخاطر والوقت',access:'الوصول',other:'أدلة أخرى'};
function groupedEvidence(state,scenario){const groups={};state.evidence.forEach(id=>{const e=evidenceFor(id,scenario,state),key=e.dimension||'other';(groups[key]??=[]).push({id,e})});return Object.entries(groups).map(([dimension,items])=>`<section class="evidence-group"><h3>${dimensionLabel[dimension]||dimension}</h3>${items.map(({id,e})=>{const selected=state.conclusionEvidence.includes(id),classification=state.evidenceSort[id]==='ctrl'?'سلطة':state.evidenceSort[id]==='ind'?'استقلال':'مختلط/سياق';return `<label class="evidence-pick"><input type="checkbox" value="${escapeHTML(id)}" ${selected?'checked':''}><span><b>${escapeHTML(e.title)}</b><small>تصنيفك السابق: ${classification}</small></span></label><details class="evidence-detail"><summary>عرض تفاصيل الدليل</summary><p class="small">${escapeHTML(e.text)}</p></details>`}).join('')}</section>`).join('')}
export function render(root){const state=getState(),scenario=scenarios[state.scenarioKey],minimum=Math.min(2,state.evidence.length),axesCount=analysisAxes(state).length;root.innerHTML=`<div class="panel"><div class="task-now"><span>🎯 مهمتك الآن</span><b>اكتب استنتاجك واربطه بدليلين على الأقل.</b></div><div class="notice info"><b>كيف تستخدم الدرجة؟</b> 30 نقطة لأسئلة العلاقة، 30 لتصنيف الأدلة، و40 لخريطة السلطة. نصك لا يُقيّم آليًا، لكن لا يمكن إنهاء التحليل دون اختيار ${minimum} ${minimum===1?'دليل':'دليلين'} يدعمان حجتك.</div><label for="analysis"><b>استنتاجك التحليلي</b></label><textarea id="analysis" class="analysis-input" rows="6" placeholder="أين تتركز السلطة؟ ما الذي بقي للعامل من استقلال؟ وما الأدلة التي تقودك إلى ذلك؟">${escapeHTML(state.analysisText)}</textarea><h2>اختر الأدلة التي تستند إليها</h2><p class="small muted">تعرض القائمة العنوان وتصنيفك السابق فقط؛ افتح التفاصيل عند الحاجة بدل إعادة قراءة كل النصوص دفعة واحدة.</p>${groupedEvidence(state,scenario)}<div class="actions"><button class="btn" id="finish">إظهار النتيجة</button></div></div>`;document.getElementById('analysis').oninput=e=>patch({analysisText:e.target.value});root.querySelectorAll('input[type=checkbox]').forEach(c=>c.onchange=()=>patch({conclusionEvidence:[...root.querySelectorAll('input[type=checkbox]:checked')].map(i=>i.value)}));document.getElementById('finish').onclick=()=>{const current=getState(),text=current.analysisText.trim();if(!powerMapComplete(current)){alert(`ارجع إلى خريطة السلطة ووزع المحاور ${axesCount} الظاهرة في هذه الجولة قبل إظهار النتيجة.`);return}if(text.length<25){alert('اكتب استنتاجًا مختصرًا من 25 حرفًا على الأقل قبل إظهار النتيجة.');return}if(current.conclusionEvidence.length<minimum){alert(`اختر ${minimum} ${minimum===1?'دليل':'دليلين'} على الأقل لدعم استنتاجك.`);return}const missing=current.evidence.filter(id=>!current.evidenceSort[id]);if(missing.length){alert('ارجع إلى تصنيف الأدلة وصنف جميع الأدلة قبل إظهار النتيجة.');return}patch({resultData:scoreAnalysis(scenario,current),realFinishedAt:Date.now(),stage:11});location.href=href('result')}}
