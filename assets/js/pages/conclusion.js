import {scenarios} from '../data/scenarios.js';
import {getState,patch} from '../core/state.js';
import {href} from '../core/routes.js';
import {escapeHTML} from '../core/html.js';
import {evidenceFor} from '../domain/evidence.js';
import {powerMapComplete,scoreAnalysis,analysisAxes} from '../domain/analysis.js';

const dimensionLabel={contract:'العقد',price:'السعر',allocation:'توزيع العمل',monitoring:'المراقبة',risk:'المخاطر والوقت',access:'الوصول',other:'أدلة أخرى'};
function groupedEvidence(state,scenario){const groups={};state.evidence.forEach(id=>{const e=evidenceFor(id,scenario,state),key=e.dimension||'other';(groups[key]??=[]).push({id,e})});return Object.entries(groups).map(([dimension,items])=>`<section class="evidence-group"><h3>${dimensionLabel[dimension]||dimension}</h3>${items.map(({id,e})=>{const role=state.conclusionEvidence.includes(id)?'support':state.conclusionCounterEvidence.includes(id)?'counter':'unused',classification=state.evidenceSort[id]==='ctrl'?'سلطة':state.evidenceSort[id]==='ind'?'استقلال':'مختلط/سياق';return `<div class="evidence-pick"><span><b>${escapeHTML(e.title)}</b><small>تصنيفك السابق: ${classification}</small></span><label class="small">دوره في استنتاجك <select data-evidence-role="${escapeHTML(id)}"><option value="unused" ${role==='unused'?'selected':''}>غير مستخدم</option><option value="support" ${role==='support'?'selected':''}>يدعم الاستنتاج</option><option value="counter" ${role==='counter'?'selected':''}>يحدّه أو يعقده</option></select></label></div><details class="evidence-detail"><summary>عرض تفاصيل الدليل</summary><p class="small">${escapeHTML(e.text)}</p></details>`}).join('')}</section>`).join('')}
function updateRoles(root){const support=[],counter=[];root.querySelectorAll('[data-evidence-role]').forEach(select=>{if(select.value==='support')support.push(select.dataset.evidenceRole);if(select.value==='counter')counter.push(select.dataset.evidenceRole)});patch({conclusionEvidence:support,conclusionCounterEvidence:counter})}
export function render(root){const state=getState(),scenario=scenarios[state.scenarioKey],minimum=Math.min(2,state.evidence.length),needsCounter=state.evidence.length>=3,axesCount=analysisAxes(state).length;root.innerHTML=`<div class="panel"><div class="task-now"><span>🎯 مهمتك الآن</span><b>اكتب استنتاجك، اربطه بدليلين على الأقل، وحدد دليلًا يحد حجتك أو يعقدها عندما تتوفر أدلة كافية.</b></div><div class="notice info"><b>كيف تستخدم الدرجة؟</b> 30 نقطة لأسئلة العلاقة، 30 لتصنيف الأدلة، و40 لترتيب السلطة في خريطة القوة. نصك لا يُقيّم آليًا؛ المطلوب هنا بناء حجة قابلة للتتبع بدل مكافأة صياغة لغوية بعينها.</div><label for="analysis"><b>استنتاجك التحليلي</b></label><textarea id="analysis" class="analysis-input" rows="6" placeholder="أين تتركز السلطة؟ ما الذي بقي للعامل من استقلال؟ ما الأدلة التي تدعم ذلك، وما الدليل الذي يجعل النتيجة أقل بساطة؟">${escapeHTML(state.analysisText)}</textarea><h2>اربط الأدلة بالحجة</h2><p class="small muted">لكل دليل اختر: يدعم استنتاجك، أو يحدّه/يعقده، أو لا تستخدمه. لا يمكن وضع الدليل نفسه في الدورين.</p>${groupedEvidence(state,scenario)}<div class="actions"><button class="btn" id="finish">إظهار النتيجة</button></div></div>`;document.getElementById('analysis').oninput=e=>patch({analysisText:e.target.value});root.querySelectorAll('[data-evidence-role]').forEach(select=>select.onchange=()=>updateRoles(root));document.getElementById('finish').onclick=()=>{const current=getState(),text=current.analysisText.trim();if(!powerMapComplete(current)){alert(`ارجع إلى خريطة السلطة ووزع المحاور ${axesCount} الظاهرة في هذه الجولة قبل إظهار النتيجة.`);return}if(text.length<25){alert('اكتب استنتاجًا مختصرًا من 25 حرفًا على الأقل قبل إظهار النتيجة.');return}if(current.conclusionEvidence.length<minimum){alert(`اختر ${minimum} ${minimum===1?'دليلًا':'دليلين'} على الأقل يدعمان استنتاجك.`);return}if(needsCounter&&current.conclusionCounterEvidence.length<1){alert('حدد دليلًا واحدًا على الأقل يحد استنتاجك أو يعقده؛ الهدف تجنب حجة أحادية الجانب.');return}const overlap=current.conclusionEvidence.filter(id=>current.conclusionCounterEvidence.includes(id));if(overlap.length){alert('لا يمكن استخدام الدليل نفسه كدعم وكدليل مضاد في الوقت نفسه.');return}const missing=current.evidence.filter(id=>!current.evidenceSort[id]);if(missing.length){alert('ارجع إلى تصنيف الأدلة وصنف جميع الأدلة قبل إظهار النتيجة.');return}patch({resultData:scoreAnalysis(scenario,current),realFinishedAt:Date.now(),stage:11});location.href=href('result')}}
