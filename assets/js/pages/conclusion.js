import {scenarios} from '../data/scenarios.js';
import {getState,patch} from '../core/state.js';
import {href} from '../core/routes.js';
import {escapeHTML} from '../core/html.js';
import {evidenceFor} from '../domain/evidence.js';
import {powerMapComplete,scoreAnalysis} from '../domain/analysis.js';

export function render(root){const state=getState(),scenario=scenarios[state.scenarioKey];root.innerHTML=`<div class="panel"><div class="task-now"><span>🎯 مهمتك الآن</span><b>اكتب استنتاجك واربطه بالأدلة التي تراها الأهم.</b></div><div class="notice info"><b>كيف تستخدم الدرجة؟</b> 30 نقطة لأسئلة العلاقة، 30 لتصنيف الأدلة، و40 لخريطة السلطة. نصك والأدلة التي ترفقها لا يحصلان على نقاط آلية؛ سيظهران في النتيجة بوصفهما حجتك أنت.</div><label for="analysis"><b>استنتاجك التحليلي</b></label><textarea id="analysis" class="analysis-input" rows="6" placeholder="أين تتركز السلطة؟ ما الذي بقي للعامل من استقلال؟ وما الأدلة التي تقودك إلى ذلك؟">${escapeHTML(state.analysisText)}</textarea><h3>الأدلة التي تريد الاستناد إليها في استنتاجك</h3><p class="small muted">اختر الأدلة التي تدعم حجتك. ستظهر تحت نصك في صفحة النتيجة.</p><div class="evidence-grid">${state.evidence.map(id=>{const evidence=evidenceFor(id,scenario,state);return `<label class="doc"><input type="checkbox" value="${escapeHTML(id)}" ${state.conclusionEvidence.includes(id)?'checked':''}> <b>${escapeHTML(evidence.title)}</b><p class="small">${escapeHTML(evidence.text)}</p></label>`}).join('')}</div><div class="actions"><button class="btn" id="finish">إظهار النتيجة</button></div></div>`;document.getElementById('analysis').oninput=event=>patch({analysisText:event.target.value});root.querySelectorAll('input[type=checkbox]').forEach(checkbox=>checkbox.onchange=()=>patch({conclusionEvidence:[...root.querySelectorAll('input[type=checkbox]:checked')].map(input=>input.value)}));document.getElementById('finish').onclick=()=>{const current=getState(),text=current.analysisText.trim();if(!powerMapComplete(current)){alert('ارجع إلى خريطة السلطة ووزع المحاور الستة قبل إظهار النتيجة.');return}if(text.length<25){alert('اكتب استنتاجًا مختصرًا من 25 حرفًا على الأقل قبل إظهار النتيجة.');return}const missing=current.evidence.filter(id=>!current.evidenceSort[id]);if(missing.length){alert('ارجع إلى تصنيف الأدلة وصنف جميع الأدلة قبل إظهار النتيجة.');return}patch({resultData:scoreAnalysis(scenario,current),realFinishedAt:Date.now(),stage:11});location.href=href('result')}}
