import {scenarios} from '../data/scenarios.js';import {getState,patch,addEvidence,addLog,money} from '../core/state.js';import {href} from '../core/routes.js';
function build(sc,s){const c=sc.costs,clientPaid=s.clientPaid,contracted=s.grossWorker,platformService=Math.max(0,clientPaid-contracted),mediator=Math.min(.22,contracted*.03),transfer=c.transfer,operating=c.internet+c.electricity+c.device,workerBeforeCosts=Math.max(0,contracted-s.hold-mediator-transfer),net=Math.max(0,workerBeforeCosts-operating);return {clientPaid,contracted,platformService,mediator,transfer,hold:s.hold,operating,workerBeforeCosts,net}}
export async function render(root){const s=getState(),sc=scenarios[s.scenarioKey],p=s.payment||build(sc,s);if(!s.payment){patch({payment:p});addEvidence('payment');addLog('ظهرت تسوية المدفوعات',`فواتير العملاء ${money(p.clientPaid)}، والمقابل المتفق عليه ${money(p.contracted)}، والصافي بعد تكاليف العامل ${money(p.net)}.`)}const now=getState(),pct=Math.round(now.unpaidTime/Math.max(1,now.time)*100),rate=now.time?p.net/now.time*60:0;root.innerHTML=`<div class="panel"><div class="instruction"><span class="n">11</span><div><b>من أين جاءت أموالك؟</b><small>قارن بين فواتير العميل والمقابل الذي وافقت عليه والصافي بعد تكاليفك.</small></div></div><h2>سلسلة الدفع</h2><div class="notice info">القيم المالية افتراضية ومصممة لأغراض التدريب.</div><div class="grid-2"><div class="receipt"><div class="receipt-row"><span>فواتير العملاء</span><b>${money(p.clientPaid)}</b></div><div class="receipt-row"><span>فرق الخدمة/إيراد المنصة قبل مصروفاتها</span><b>${money(p.platformService)}</b></div><div class="receipt-row"><span>المقابل المتفق عليه للعامل</span><b>${money(p.contracted)}</b></div><div class="receipt-row"><span>حجز نزاع جودة</span><b>-${money(p.hold)}</b></div><div class="receipt-row"><span>رسوم الدفع والتحويل</span><b>-${money(p.mediator+p.transfer)}</b></div><div class="receipt-row"><span>تكاليف تشغيل العامل</span><b>-${money(p.operating)}</b></div><div class="receipt-row"><span><b>الصافي</b></span><b>${money(p.net)}</b></div><div class="receipt-row"><span>إجمالي وقت الوردية</span><b>${now.time} د</b></div><div class="receipt-row"><span>وقت مدفوع</span><b>${now.paidTime} د</b></div><div class="receipt-row" style="color:var(--bad)"><span><b>وقت غير مدفوع</b></span><b>${now.unpaidTime} د · ${pct}%</b></div></div><div class="card" style="text-align:center"><small class="muted">الصافي الفعلي للساعة بعد تكاليف العامل</small><div class="price">${money(rate)}</div><p class="small muted">الصافي ÷ إجمالي وقت الوردية.</p></div></div><div class="actions"><button class="btn" id="next">إنهاء تسوية الوردية</button></div></div>`;document.getElementById('next').onclick=()=>location.href=href('access')}
