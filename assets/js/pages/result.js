import {scenarios,axes,parties,partyNames,evidenceTemplates} from '../data/scenarios.js';import {getState,money} from '../core/state.js';import {href} from '../core/routes.js';import {archiveResult} from '../core/storage.js';
function outcomeLabel(o){return o==='suspended'?'تعليق الحساب العام':o==='project'?'استبعاد من المشروع':'تحذير مع استمرار الحساب'}
export async function render(root){const s=getState(),sc=scenarios[s.scenarioKey],r=s.resultData||{score:0},pct=Math.round(s.unpaidTime/Math.max(1,s.time)*100),elapsed=s.realStartedAt?Math.max(1,Math.round(((s.realFinishedAt||Date.now())-s.realStartedAt)/60000)):0;root.innerHTML=`<div class="panel"><div class="result-banner"><div class="eyebrow" style="color:#bfe5f2">اتساق التحليل مع أدلة السيناريو</div><div class="score">${r.score}/100</div><div>${r.score>=88?'اتساق مرتفع جدًا':r.score>=74?'اتساق جيد مع مساحة لتحسين بعض التوزيعات':'توجد نقاط تستحق إعادة الفحص'}</div><div class="small">الدرجة تقيس الاتساق داخل هذه المحاكاة فقط، ولا تمثل حكمًا قانونيًا.</div></div><div class="grid-4" style="margin-top:12px"><div class="metric"><small>زمن المشاركة الفعلي</small><b>${elapsed} د</b></div><div class="metric"><small>زمن الوردية</small><b>${s.time} د</b></div><div class="metric"><small>الوقت غير المدفوع</small><b>${s.unpaidTime} د · ${pct}%</b></div><div class="metric"><small>نتيجة الوصول</small><b>${outcomeLabel(s.accountOutcome)}</b></div></div><div class="notice"><b>تنبيه قانوني:</b> اللعبة تحلل الوقائع وتوزيع وظائف السيطرة ولا تصدر توصيفًا قانونيًا نهائيًا.</div><h3>العقد مقابل الواقع</h3><div class="grid-2"><div class="card"><h3>ما يقوله العقد</h3><p>• مقدم الخدمة مستقل.</p><p>• يختار وقت العمل.</p><p>• يستطيع العمل لجهات أخرى.</p><p>• يستخدم أدواته ويتحمل نفقاته.</p></div><div class="card"><h3>ما حدث في الوردية</h3><p>• ${sc.allocationMechanism}</p><p>• المنصة جمعت مؤشرات أداء مرتبطة بالمشروع.</p><p>• العميل أثر في معيار الجودة.</p><p>• ${s.accountOutcome==='suspended'?'المنصة علقت الوصول إلى السوق العام.':s.accountOutcome==='project'?'العميل أثر في المشروع بينما بقي الحساب تحت إدارة المنصة.':'المنصة عدلت أولوية الوصول مع بقاء الحساب نشطًا.'}</p></div></div><h3>خريطة السلطة</h3>${axes.map(a=>{const p=s.power[a.id];return `<div class="power-card"><b>${a.title}</b><div class="stacked">${parties.map(x=>`<span class="seg ${x}" style="width:${p[x]}%"></span>`).join('')}</div><div class="small">${parties.map(x=>`${partyNames[x]} ${p[x]}`).join(' · ')}</div></div>`}).join('')}<h3>استنتاجك</h3><div class="notice info">${s.analysisText}</div><div class="actions"><button class="btn" id="rights">اربط التجربة بالحقوق</button><button class="btn secondary" id="archive">حفظ النتيجة للمقارنة</button><button class="btn ghost" id="restartScenario">تجربة حالة أخرى</button></div></div>`;document.getElementById('rights').onclick=()=>location.href=href('rights');document.getElementById('archive').onclick=()=>{archiveResult({version:'TaskBridge v2.0.0',scenario:s.scenarioKey,scenarioName:sc.role,score:r.score,outcome:s.accountOutcome,simMinutes:s.time,paidMinutes:s.paidTime,unpaidMinutes:s.unpaidTime,power:s.power,answers:s.answers,analysis:s.analysisText,createdAt:new Date().toISOString()});alert('تم حفظ النتيجة للمقارنة على هذا الجهاز.')};document.getElementById('restartScenario').onclick=()=>location.href=href('scenario')}
